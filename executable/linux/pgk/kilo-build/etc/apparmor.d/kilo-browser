abi <abi/4.0>,
include <tunables/global>

profile kilo-browser /opt/kilo-browser/kilo flags=(unconfined) {
    userns,

    # Site-specific additions and overrides.
    include if exists <local/kilo-browser>
}